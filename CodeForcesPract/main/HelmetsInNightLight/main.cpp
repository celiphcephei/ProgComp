#include <bits/stdc++.h>
using namespace std;
vector<int> a, b;

bool good(int n){
    return n >= 
}

void solve(){
    int n, p, an{0}; scanf("%d %d", &n, &p);
    for(int i{0}; i < n; ++i)
        scanf("%d", &a[i]);
    for(int i{0}; i < n; ++i)
        scanf("%d", &b[i]);
    int num = n * p;

}

int main(){
    int t; scanf("%d", &t);
    while(t--) solve();
    return 0;
}