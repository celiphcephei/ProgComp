#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    while(n--){
        vector<char> letras(n);
        int x;
        cin >> x;
        while(x--){
            string name;
            cin >> name;
            char letra = name[0];
            letras.push_back(letra)
        }
    }
    cout 
    return 0;
}